default: &default
  adapter: postgresql
  pool: 5
  timeout: 5000
  database: oskimart_development
  # eric's db stuff
  username: Zheryu
  password: password

development:
  <<: *default

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &test
  <<: *default
  database: oskimart_test

production:
  <<: *default
  database: oskimart_production
cucumber:
  <<: *test