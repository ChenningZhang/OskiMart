<% if @review.errors.any? %>
	<div id='error_explanation'>
		<div class='alert alert-danger'>
			Oops, something went wrong while adding a new review.
		</div>
		<ul>
			<% @review.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>